name: Upgrade LAME Pinball to v23
on:
  push:
    paths:
      - 'upgrade_v23.py'
permissions:
  contents: write
jobs:
  upgrade:
    runs-on: ubuntu-latest
    steps:
      - name: Run actions/checkout@v4
        uses: actions/checkout@v4
      - name: Apply v23 upgrade
        run: python upgrade_v23.py
      - name: Commit upgraded game
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          git commit -m "Upgrade LAME Pinball to v23" || exit 0
          git push