function hitBoxBumper(){const bx=195,by=700,bw=210,bh=145,pad=ball.r+5;if(ball.x<bx-pad||ball.x>bx+bw+pad||ball.y<by-pad||ball.y>by+bh+pad)return;ball.vy=-Math.max(10.5,Math.abs(ball.vy)*1.18+4.2);ball.vx+=((ball.x-300)/110)*3;ball.y=by-pad;capSpeed();boxHitFlash=12;if(performance.now()-boxHitAt>120){boxHitAt=performance.now();sfxHit(2);addScore(900,300,735);setStatus("LAME BOX BUMPER! +900")}}